import React from 'react';

const AutoPop = (props) => (
    <div>
        <button className="button button--link" onClick={props.handlePopulateOptions}>Need some ideas?</button>
    </div>
);

export default AutoPop;